/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	addDecoderSizePrefix,
	addEncoderSizePrefix,
	combineCodec,
	fixDecoderSize,
	fixEncoderSize,
	getArrayDecoder,
	getArrayEncoder,
	getBytesDecoder,
	getBytesEncoder,
	getI64Decoder,
	getI64Encoder,
	getStructDecoder,
	getStructEncoder,
	getU32Decoder,
	getU32Encoder,
	getU64Decoder,
	getU64Encoder,
	getUtf8Decoder,
	getUtf8Encoder,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	transformEncoder,
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type Codec,
	type Decoder,
	type Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount,
	type WritableSignerAccount
} from '@solana/kit';
import {
	getAccountMetaFactory,
	getAddressFromResolvedInstructionAccount,
	getNonNullResolvedInstructionInput,
	type ResolvedInstructionAccount
} from '@solana/program-client-core';
import { findChallengePda } from '../pdas';
import { NEOFIT_PROGRAM_ADDRESS } from '../programs';
import {
	getExerciseRequirementDecoder,
	getExerciseRequirementEncoder,
	type ExerciseRequirement,
	type ExerciseRequirementArgs
} from '../types';

export const CREATE_CHALLENGE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
	170, 244, 47, 1, 1, 15, 173, 239
]);

export function getCreateChallengeDiscriminatorBytes(): ReadonlyUint8Array {
	return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_CHALLENGE_DISCRIMINATOR);
}

export type CreateChallengeInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountChallenge extends string | AccountMeta<string> = string,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
	TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
	InstructionWithData<ReadonlyUint8Array> &
	InstructionWithAccounts<
		[
			TAccountChallenge extends string ? WritableAccount<TAccountChallenge> : TAccountChallenge,
			TAccountAuthority extends string
				? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			TAccountSystemProgram extends string
				? ReadonlyAccount<TAccountSystemProgram>
				: TAccountSystemProgram,
			...TRemainingAccounts
		]
	>;

export type CreateChallengeInstructionData = {
	discriminator: ReadonlyUint8Array;
	title: string;
	requirements: Array<ExerciseRequirement>;
	entryFee: bigint;
	deadline: bigint;
	nonce: bigint;
};

export type CreateChallengeInstructionDataArgs = {
	title: string;
	requirements: Array<ExerciseRequirementArgs>;
	entryFee: number | bigint;
	deadline: number | bigint;
	nonce: number | bigint;
};

export function getCreateChallengeInstructionDataEncoder(): Encoder<CreateChallengeInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([
			['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
			['title', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
			['requirements', getArrayEncoder(getExerciseRequirementEncoder())],
			['entryFee', getU64Encoder()],
			['deadline', getI64Encoder()],
			['nonce', getU64Encoder()]
		]),
		(value) => ({ ...value, discriminator: CREATE_CHALLENGE_DISCRIMINATOR })
	);
}

export function getCreateChallengeInstructionDataDecoder(): Decoder<CreateChallengeInstructionData> {
	return getStructDecoder([
		['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
		['title', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
		['requirements', getArrayDecoder(getExerciseRequirementDecoder())],
		['entryFee', getU64Decoder()],
		['deadline', getI64Decoder()],
		['nonce', getU64Decoder()]
	]);
}

export function getCreateChallengeInstructionDataCodec(): Codec<
	CreateChallengeInstructionDataArgs,
	CreateChallengeInstructionData
> {
	return combineCodec(
		getCreateChallengeInstructionDataEncoder(),
		getCreateChallengeInstructionDataDecoder()
	);
}

export type CreateChallengeAsyncInput<
	TAccountChallenge extends string = string,
	TAccountAuthority extends string = string,
	TAccountSystemProgram extends string = string
> = {
	challenge?: Address<TAccountChallenge>;
	authority: TransactionSigner<TAccountAuthority>;
	systemProgram?: Address<TAccountSystemProgram>;
	title: CreateChallengeInstructionDataArgs['title'];
	requirements: CreateChallengeInstructionDataArgs['requirements'];
	entryFee: CreateChallengeInstructionDataArgs['entryFee'];
	deadline: CreateChallengeInstructionDataArgs['deadline'];
	nonce: CreateChallengeInstructionDataArgs['nonce'];
};

export async function getCreateChallengeInstructionAsync<
	TAccountChallenge extends string,
	TAccountAuthority extends string,
	TAccountSystemProgram extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: CreateChallengeAsyncInput<TAccountChallenge, TAccountAuthority, TAccountSystemProgram>,
	config?: { programAddress?: TProgramAddress }
): Promise<
	CreateChallengeInstruction<
		TProgramAddress,
		TAccountChallenge,
		TAccountAuthority,
		TAccountSystemProgram
	>
> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		challenge: { value: input.challenge ?? null, isWritable: true },
		authority: { value: input.authority ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Original args.
	const args = { ...input };

	// Resolve default values.
	if (!accounts.challenge.value) {
		accounts.challenge.value = await findChallengePda({
			authority: getAddressFromResolvedInstructionAccount('authority', accounts.authority.value),
			nonce: getNonNullResolvedInstructionInput('nonce', args.nonce)
		});
	}
	if (!accounts.systemProgram.value) {
		accounts.systemProgram.value =
			'11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('challenge', accounts.challenge),
			getAccountMeta('authority', accounts.authority),
			getAccountMeta('systemProgram', accounts.systemProgram)
		],
		data: getCreateChallengeInstructionDataEncoder().encode(
			args as CreateChallengeInstructionDataArgs
		),
		programAddress
	} as CreateChallengeInstruction<
		TProgramAddress,
		TAccountChallenge,
		TAccountAuthority,
		TAccountSystemProgram
	>);
}

export type CreateChallengeInput<
	TAccountChallenge extends string = string,
	TAccountAuthority extends string = string,
	TAccountSystemProgram extends string = string
> = {
	challenge: Address<TAccountChallenge>;
	authority: TransactionSigner<TAccountAuthority>;
	systemProgram?: Address<TAccountSystemProgram>;
	title: CreateChallengeInstructionDataArgs['title'];
	requirements: CreateChallengeInstructionDataArgs['requirements'];
	entryFee: CreateChallengeInstructionDataArgs['entryFee'];
	deadline: CreateChallengeInstructionDataArgs['deadline'];
	nonce: CreateChallengeInstructionDataArgs['nonce'];
};

export function getCreateChallengeInstruction<
	TAccountChallenge extends string,
	TAccountAuthority extends string,
	TAccountSystemProgram extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: CreateChallengeInput<TAccountChallenge, TAccountAuthority, TAccountSystemProgram>,
	config?: { programAddress?: TProgramAddress }
): CreateChallengeInstruction<
	TProgramAddress,
	TAccountChallenge,
	TAccountAuthority,
	TAccountSystemProgram
> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		challenge: { value: input.challenge ?? null, isWritable: true },
		authority: { value: input.authority ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Original args.
	const args = { ...input };

	// Resolve default values.
	if (!accounts.systemProgram.value) {
		accounts.systemProgram.value =
			'11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('challenge', accounts.challenge),
			getAccountMeta('authority', accounts.authority),
			getAccountMeta('systemProgram', accounts.systemProgram)
		],
		data: getCreateChallengeInstructionDataEncoder().encode(
			args as CreateChallengeInstructionDataArgs
		),
		programAddress
	} as CreateChallengeInstruction<
		TProgramAddress,
		TAccountChallenge,
		TAccountAuthority,
		TAccountSystemProgram
	>);
}

export type ParsedCreateChallengeInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
	programAddress: Address<TProgram>;
	accounts: {
		challenge: TAccountMetas[0];
		authority: TAccountMetas[1];
		systemProgram: TAccountMetas[2];
	};
	data: CreateChallengeInstructionData;
};

export function parseCreateChallengeInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[]
>(
	instruction: Instruction<TProgram> &
		InstructionWithAccounts<TAccountMetas> &
		InstructionWithData<ReadonlyUint8Array>
): ParsedCreateChallengeInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 3) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 3
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: {
			challenge: getNextAccount(),
			authority: getNextAccount(),
			systemProgram: getNextAccount()
		},
		data: getCreateChallengeInstructionDataDecoder().decode(instruction.data)
	};
}
