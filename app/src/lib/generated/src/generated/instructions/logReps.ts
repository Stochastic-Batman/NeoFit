/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	combineCodec,
	fixDecoderSize,
	fixEncoderSize,
	getBytesDecoder,
	getBytesEncoder,
	getStructDecoder,
	getStructEncoder,
	getU32Decoder,
	getU32Encoder,
	getU8Decoder,
	getU8Encoder,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	transformEncoder,
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlySignerAccount,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount
} from '@solana/kit';
import {
	getAccountMetaFactory,
	getAddressFromResolvedInstructionAccount,
	type ResolvedInstructionAccount
} from '@solana/program-client-core';
import { findUserProfilePda } from '../pdas';
import { NEOFIT_PROGRAM_ADDRESS } from '../programs';

export const LOG_REPS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
	161, 17, 60, 30, 17, 71, 252, 184
]);

export function getLogRepsDiscriminatorBytes(): ReadonlyUint8Array {
	return fixEncoderSize(getBytesEncoder(), 8).encode(LOG_REPS_DISCRIMINATOR);
}

export type LogRepsInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountUserProfile extends string | AccountMeta<string> = string,
	TAccountEnrollment extends string | AccountMeta<string> = string,
	TAccountChallenge extends string | AccountMeta<string> = string,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
	InstructionWithData<ReadonlyUint8Array> &
	InstructionWithAccounts<
		[
			TAccountUserProfile extends string
				? WritableAccount<TAccountUserProfile>
				: TAccountUserProfile,
			TAccountEnrollment extends string ? WritableAccount<TAccountEnrollment> : TAccountEnrollment,
			TAccountChallenge extends string ? WritableAccount<TAccountChallenge> : TAccountChallenge,
			TAccountAuthority extends string
				? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			...TRemainingAccounts
		]
	>;

export type LogRepsInstructionData = {
	discriminator: ReadonlyUint8Array;
	exerciseId: number;
	count: number;
};

export type LogRepsInstructionDataArgs = { exerciseId: number; count: number };

export function getLogRepsInstructionDataEncoder(): FixedSizeEncoder<LogRepsInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([
			['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
			['exerciseId', getU8Encoder()],
			['count', getU32Encoder()]
		]),
		(value) => ({ ...value, discriminator: LOG_REPS_DISCRIMINATOR })
	);
}

export function getLogRepsInstructionDataDecoder(): FixedSizeDecoder<LogRepsInstructionData> {
	return getStructDecoder([
		['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
		['exerciseId', getU8Decoder()],
		['count', getU32Decoder()]
	]);
}

export function getLogRepsInstructionDataCodec(): FixedSizeCodec<
	LogRepsInstructionDataArgs,
	LogRepsInstructionData
> {
	return combineCodec(getLogRepsInstructionDataEncoder(), getLogRepsInstructionDataDecoder());
}

export type LogRepsAsyncInput<
	TAccountUserProfile extends string = string,
	TAccountEnrollment extends string = string,
	TAccountChallenge extends string = string,
	TAccountAuthority extends string = string
> = {
	userProfile?: Address<TAccountUserProfile>;
	enrollment?: Address<TAccountEnrollment>;
	challenge?: Address<TAccountChallenge>;
	authority: TransactionSigner<TAccountAuthority>;
	exerciseId: LogRepsInstructionDataArgs['exerciseId'];
	count: LogRepsInstructionDataArgs['count'];
};

export async function getLogRepsInstructionAsync<
	TAccountUserProfile extends string,
	TAccountEnrollment extends string,
	TAccountChallenge extends string,
	TAccountAuthority extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: LogRepsAsyncInput<
		TAccountUserProfile,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountAuthority
	>,
	config?: { programAddress?: TProgramAddress }
): Promise<
	LogRepsInstruction<
		TProgramAddress,
		TAccountUserProfile,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountAuthority
	>
> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		userProfile: { value: input.userProfile ?? null, isWritable: true },
		enrollment: { value: input.enrollment ?? null, isWritable: true },
		challenge: { value: input.challenge ?? null, isWritable: true },
		authority: { value: input.authority ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Original args.
	const args = { ...input };

	// Resolve default values.
	if (!accounts.userProfile.value) {
		accounts.userProfile.value = await findUserProfilePda({
			authority: getAddressFromResolvedInstructionAccount('authority', accounts.authority.value)
		});
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('userProfile', accounts.userProfile),
			getAccountMeta('enrollment', accounts.enrollment),
			getAccountMeta('challenge', accounts.challenge),
			getAccountMeta('authority', accounts.authority)
		],
		data: getLogRepsInstructionDataEncoder().encode(args as LogRepsInstructionDataArgs),
		programAddress
	} as LogRepsInstruction<
		TProgramAddress,
		TAccountUserProfile,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountAuthority
	>);
}

export type LogRepsInput<
	TAccountUserProfile extends string = string,
	TAccountEnrollment extends string = string,
	TAccountChallenge extends string = string,
	TAccountAuthority extends string = string
> = {
	userProfile: Address<TAccountUserProfile>;
	enrollment?: Address<TAccountEnrollment>;
	challenge?: Address<TAccountChallenge>;
	authority: TransactionSigner<TAccountAuthority>;
	exerciseId: LogRepsInstructionDataArgs['exerciseId'];
	count: LogRepsInstructionDataArgs['count'];
};

export function getLogRepsInstruction<
	TAccountUserProfile extends string,
	TAccountEnrollment extends string,
	TAccountChallenge extends string,
	TAccountAuthority extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: LogRepsInput<
		TAccountUserProfile,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountAuthority
	>,
	config?: { programAddress?: TProgramAddress }
): LogRepsInstruction<
	TProgramAddress,
	TAccountUserProfile,
	TAccountEnrollment,
	TAccountChallenge,
	TAccountAuthority
> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		userProfile: { value: input.userProfile ?? null, isWritable: true },
		enrollment: { value: input.enrollment ?? null, isWritable: true },
		challenge: { value: input.challenge ?? null, isWritable: true },
		authority: { value: input.authority ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Original args.
	const args = { ...input };

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('userProfile', accounts.userProfile),
			getAccountMeta('enrollment', accounts.enrollment),
			getAccountMeta('challenge', accounts.challenge),
			getAccountMeta('authority', accounts.authority)
		],
		data: getLogRepsInstructionDataEncoder().encode(args as LogRepsInstructionDataArgs),
		programAddress
	} as LogRepsInstruction<
		TProgramAddress,
		TAccountUserProfile,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountAuthority
	>);
}

export type ParsedLogRepsInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
	programAddress: Address<TProgram>;
	accounts: {
		userProfile: TAccountMetas[0];
		enrollment?: TAccountMetas[1] | undefined;
		challenge?: TAccountMetas[2] | undefined;
		authority: TAccountMetas[3];
	};
	data: LogRepsInstructionData;
};

export function parseLogRepsInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[]
>(
	instruction: Instruction<TProgram> &
		InstructionWithAccounts<TAccountMetas> &
		InstructionWithData<ReadonlyUint8Array>
): ParsedLogRepsInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 4) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 4
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	const getNextOptionalAccount = () => {
		const accountMeta = getNextAccount();
		return accountMeta.address === NEOFIT_PROGRAM_ADDRESS ? undefined : accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: {
			userProfile: getNextAccount(),
			enrollment: getNextOptionalAccount(),
			challenge: getNextOptionalAccount(),
			authority: getNextAccount()
		},
		data: getLogRepsInstructionDataDecoder().decode(instruction.data)
	};
}
