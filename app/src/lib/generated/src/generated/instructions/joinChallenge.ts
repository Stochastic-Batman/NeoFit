/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	combineCodec,
	fixDecoderSize,
	fixEncoderSize,
	getBytesDecoder,
	getBytesEncoder,
	getStructDecoder,
	getStructEncoder,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	transformEncoder,
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount,
	type WritableSignerAccount
} from '@solana/kit';
import {
	getAccountMetaFactory,
	getAddressFromResolvedInstructionAccount,
	type ResolvedInstructionAccount
} from '@solana/program-client-core';
import { findEnrollmentPda, findUserProfilePda } from '../pdas';
import { NEOFIT_PROGRAM_ADDRESS } from '../programs';

export const JOIN_CHALLENGE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
	41, 104, 214, 73, 32, 168, 76, 79
]);

export function getJoinChallengeDiscriminatorBytes(): ReadonlyUint8Array {
	return fixEncoderSize(getBytesEncoder(), 8).encode(JOIN_CHALLENGE_DISCRIMINATOR);
}

export type JoinChallengeInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountEnrollment extends string | AccountMeta<string> = string,
	TAccountChallenge extends string | AccountMeta<string> = string,
	TAccountUserProfile extends string | AccountMeta<string> = string,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
	TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
	InstructionWithData<ReadonlyUint8Array> &
	InstructionWithAccounts<
		[
			TAccountEnrollment extends string ? WritableAccount<TAccountEnrollment> : TAccountEnrollment,
			TAccountChallenge extends string ? WritableAccount<TAccountChallenge> : TAccountChallenge,
			TAccountUserProfile extends string
				? ReadonlyAccount<TAccountUserProfile>
				: TAccountUserProfile,
			TAccountAuthority extends string
				? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			TAccountSystemProgram extends string
				? ReadonlyAccount<TAccountSystemProgram>
				: TAccountSystemProgram,
			...TRemainingAccounts
		]
	>;

export type JoinChallengeInstructionData = { discriminator: ReadonlyUint8Array };

export type JoinChallengeInstructionDataArgs = {};

export function getJoinChallengeInstructionDataEncoder(): FixedSizeEncoder<JoinChallengeInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
		(value) => ({ ...value, discriminator: JOIN_CHALLENGE_DISCRIMINATOR })
	);
}

export function getJoinChallengeInstructionDataDecoder(): FixedSizeDecoder<JoinChallengeInstructionData> {
	return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getJoinChallengeInstructionDataCodec(): FixedSizeCodec<
	JoinChallengeInstructionDataArgs,
	JoinChallengeInstructionData
> {
	return combineCodec(
		getJoinChallengeInstructionDataEncoder(),
		getJoinChallengeInstructionDataDecoder()
	);
}

export type JoinChallengeAsyncInput<
	TAccountEnrollment extends string = string,
	TAccountChallenge extends string = string,
	TAccountUserProfile extends string = string,
	TAccountAuthority extends string = string,
	TAccountSystemProgram extends string = string
> = {
	enrollment?: Address<TAccountEnrollment>;
	challenge: Address<TAccountChallenge>;
	userProfile?: Address<TAccountUserProfile>;
	authority: TransactionSigner<TAccountAuthority>;
	systemProgram?: Address<TAccountSystemProgram>;
};

export async function getJoinChallengeInstructionAsync<
	TAccountEnrollment extends string,
	TAccountChallenge extends string,
	TAccountUserProfile extends string,
	TAccountAuthority extends string,
	TAccountSystemProgram extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: JoinChallengeAsyncInput<
		TAccountEnrollment,
		TAccountChallenge,
		TAccountUserProfile,
		TAccountAuthority,
		TAccountSystemProgram
	>,
	config?: { programAddress?: TProgramAddress }
): Promise<
	JoinChallengeInstruction<
		TProgramAddress,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountUserProfile,
		TAccountAuthority,
		TAccountSystemProgram
	>
> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		enrollment: { value: input.enrollment ?? null, isWritable: true },
		challenge: { value: input.challenge ?? null, isWritable: true },
		userProfile: { value: input.userProfile ?? null, isWritable: false },
		authority: { value: input.authority ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Resolve default values.
	if (!accounts.enrollment.value) {
		accounts.enrollment.value = await findEnrollmentPda({
			challenge: getAddressFromResolvedInstructionAccount('challenge', accounts.challenge.value),
			authority: getAddressFromResolvedInstructionAccount('authority', accounts.authority.value)
		});
	}
	if (!accounts.userProfile.value) {
		accounts.userProfile.value = await findUserProfilePda({
			authority: getAddressFromResolvedInstructionAccount('authority', accounts.authority.value)
		});
	}
	if (!accounts.systemProgram.value) {
		accounts.systemProgram.value =
			'11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('enrollment', accounts.enrollment),
			getAccountMeta('challenge', accounts.challenge),
			getAccountMeta('userProfile', accounts.userProfile),
			getAccountMeta('authority', accounts.authority),
			getAccountMeta('systemProgram', accounts.systemProgram)
		],
		data: getJoinChallengeInstructionDataEncoder().encode({}),
		programAddress
	} as JoinChallengeInstruction<
		TProgramAddress,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountUserProfile,
		TAccountAuthority,
		TAccountSystemProgram
	>);
}

export type JoinChallengeInput<
	TAccountEnrollment extends string = string,
	TAccountChallenge extends string = string,
	TAccountUserProfile extends string = string,
	TAccountAuthority extends string = string,
	TAccountSystemProgram extends string = string
> = {
	enrollment: Address<TAccountEnrollment>;
	challenge: Address<TAccountChallenge>;
	userProfile: Address<TAccountUserProfile>;
	authority: TransactionSigner<TAccountAuthority>;
	systemProgram?: Address<TAccountSystemProgram>;
};

export function getJoinChallengeInstruction<
	TAccountEnrollment extends string,
	TAccountChallenge extends string,
	TAccountUserProfile extends string,
	TAccountAuthority extends string,
	TAccountSystemProgram extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: JoinChallengeInput<
		TAccountEnrollment,
		TAccountChallenge,
		TAccountUserProfile,
		TAccountAuthority,
		TAccountSystemProgram
	>,
	config?: { programAddress?: TProgramAddress }
): JoinChallengeInstruction<
	TProgramAddress,
	TAccountEnrollment,
	TAccountChallenge,
	TAccountUserProfile,
	TAccountAuthority,
	TAccountSystemProgram
> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		enrollment: { value: input.enrollment ?? null, isWritable: true },
		challenge: { value: input.challenge ?? null, isWritable: true },
		userProfile: { value: input.userProfile ?? null, isWritable: false },
		authority: { value: input.authority ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Resolve default values.
	if (!accounts.systemProgram.value) {
		accounts.systemProgram.value =
			'11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('enrollment', accounts.enrollment),
			getAccountMeta('challenge', accounts.challenge),
			getAccountMeta('userProfile', accounts.userProfile),
			getAccountMeta('authority', accounts.authority),
			getAccountMeta('systemProgram', accounts.systemProgram)
		],
		data: getJoinChallengeInstructionDataEncoder().encode({}),
		programAddress
	} as JoinChallengeInstruction<
		TProgramAddress,
		TAccountEnrollment,
		TAccountChallenge,
		TAccountUserProfile,
		TAccountAuthority,
		TAccountSystemProgram
	>);
}

export type ParsedJoinChallengeInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
	programAddress: Address<TProgram>;
	accounts: {
		enrollment: TAccountMetas[0];
		challenge: TAccountMetas[1];
		userProfile: TAccountMetas[2];
		authority: TAccountMetas[3];
		systemProgram: TAccountMetas[4];
	};
	data: JoinChallengeInstructionData;
};

export function parseJoinChallengeInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[]
>(
	instruction: Instruction<TProgram> &
		InstructionWithAccounts<TAccountMetas> &
		InstructionWithData<ReadonlyUint8Array>
): ParsedJoinChallengeInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 5) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 5
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: {
			enrollment: getNextAccount(),
			challenge: getNextAccount(),
			userProfile: getNextAccount(),
			authority: getNextAccount(),
			systemProgram: getNextAccount()
		},
		data: getJoinChallengeInstructionDataDecoder().decode(instruction.data)
	};
}
