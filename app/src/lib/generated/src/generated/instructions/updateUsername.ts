/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	addDecoderSizePrefix,
	addEncoderSizePrefix,
	combineCodec,
	fixDecoderSize,
	fixEncoderSize,
	getBytesDecoder,
	getBytesEncoder,
	getStructDecoder,
	getStructEncoder,
	getU32Decoder,
	getU32Encoder,
	getUtf8Decoder,
	getUtf8Encoder,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	transformEncoder,
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	type Codec,
	type Decoder,
	type Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlySignerAccount,
	type ReadonlyUint8Array,
	type TransactionSigner,
	type WritableAccount
} from '@solana/kit';
import {
	getAccountMetaFactory,
	getAddressFromResolvedInstructionAccount,
	type ResolvedInstructionAccount
} from '@solana/program-client-core';
import { findUserProfilePda } from '../pdas';
import { NEOFIT_PROGRAM_ADDRESS } from '../programs';

export const UPDATE_USERNAME_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
	233, 103, 45, 8, 250, 100, 216, 251
]);

export function getUpdateUsernameDiscriminatorBytes(): ReadonlyUint8Array {
	return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_USERNAME_DISCRIMINATOR);
}

export type UpdateUsernameInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountUserProfile extends string | AccountMeta<string> = string,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = []
> = Instruction<TProgram> &
	InstructionWithData<ReadonlyUint8Array> &
	InstructionWithAccounts<
		[
			TAccountUserProfile extends string
				? WritableAccount<TAccountUserProfile>
				: TAccountUserProfile,
			TAccountAuthority extends string
				? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			...TRemainingAccounts
		]
	>;

export type UpdateUsernameInstructionData = {
	discriminator: ReadonlyUint8Array;
	newUsername: string;
};

export type UpdateUsernameInstructionDataArgs = { newUsername: string };

export function getUpdateUsernameInstructionDataEncoder(): Encoder<UpdateUsernameInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([
			['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
			['newUsername', addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())]
		]),
		(value) => ({ ...value, discriminator: UPDATE_USERNAME_DISCRIMINATOR })
	);
}

export function getUpdateUsernameInstructionDataDecoder(): Decoder<UpdateUsernameInstructionData> {
	return getStructDecoder([
		['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
		['newUsername', addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())]
	]);
}

export function getUpdateUsernameInstructionDataCodec(): Codec<
	UpdateUsernameInstructionDataArgs,
	UpdateUsernameInstructionData
> {
	return combineCodec(
		getUpdateUsernameInstructionDataEncoder(),
		getUpdateUsernameInstructionDataDecoder()
	);
}

export type UpdateUsernameAsyncInput<
	TAccountUserProfile extends string = string,
	TAccountAuthority extends string = string
> = {
	userProfile?: Address<TAccountUserProfile>;
	authority: TransactionSigner<TAccountAuthority>;
	newUsername: UpdateUsernameInstructionDataArgs['newUsername'];
};

export async function getUpdateUsernameInstructionAsync<
	TAccountUserProfile extends string,
	TAccountAuthority extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: UpdateUsernameAsyncInput<TAccountUserProfile, TAccountAuthority>,
	config?: { programAddress?: TProgramAddress }
): Promise<UpdateUsernameInstruction<TProgramAddress, TAccountUserProfile, TAccountAuthority>> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		userProfile: { value: input.userProfile ?? null, isWritable: true },
		authority: { value: input.authority ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Original args.
	const args = { ...input };

	// Resolve default values.
	if (!accounts.userProfile.value) {
		accounts.userProfile.value = await findUserProfilePda({
			authority: getAddressFromResolvedInstructionAccount('authority', accounts.authority.value)
		});
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('userProfile', accounts.userProfile),
			getAccountMeta('authority', accounts.authority)
		],
		data: getUpdateUsernameInstructionDataEncoder().encode(
			args as UpdateUsernameInstructionDataArgs
		),
		programAddress
	} as UpdateUsernameInstruction<TProgramAddress, TAccountUserProfile, TAccountAuthority>);
}

export type UpdateUsernameInput<
	TAccountUserProfile extends string = string,
	TAccountAuthority extends string = string
> = {
	userProfile: Address<TAccountUserProfile>;
	authority: TransactionSigner<TAccountAuthority>;
	newUsername: UpdateUsernameInstructionDataArgs['newUsername'];
};

export function getUpdateUsernameInstruction<
	TAccountUserProfile extends string,
	TAccountAuthority extends string,
	TProgramAddress extends Address = typeof NEOFIT_PROGRAM_ADDRESS
>(
	input: UpdateUsernameInput<TAccountUserProfile, TAccountAuthority>,
	config?: { programAddress?: TProgramAddress }
): UpdateUsernameInstruction<TProgramAddress, TAccountUserProfile, TAccountAuthority> {
	// Program address.
	const programAddress = config?.programAddress ?? NEOFIT_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		userProfile: { value: input.userProfile ?? null, isWritable: true },
		authority: { value: input.authority ?? null, isWritable: false }
	};
	const accounts = originalAccounts as Record<
		keyof typeof originalAccounts,
		ResolvedInstructionAccount
	>;

	// Original args.
	const args = { ...input };

	const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
	return Object.freeze({
		accounts: [
			getAccountMeta('userProfile', accounts.userProfile),
			getAccountMeta('authority', accounts.authority)
		],
		data: getUpdateUsernameInstructionDataEncoder().encode(
			args as UpdateUsernameInstructionDataArgs
		),
		programAddress
	} as UpdateUsernameInstruction<TProgramAddress, TAccountUserProfile, TAccountAuthority>);
}

export type ParsedUpdateUsernameInstruction<
	TProgram extends string = typeof NEOFIT_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]
> = {
	programAddress: Address<TProgram>;
	accounts: {
		userProfile: TAccountMetas[0];
		authority: TAccountMetas[1];
	};
	data: UpdateUsernameInstructionData;
};

export function parseUpdateUsernameInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[]
>(
	instruction: Instruction<TProgram> &
		InstructionWithAccounts<TAccountMetas> &
		InstructionWithData<ReadonlyUint8Array>
): ParsedUpdateUsernameInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 2) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 2
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { userProfile: getNextAccount(), authority: getNextAccount() },
		data: getUpdateUsernameInstructionDataDecoder().decode(instruction.data)
	};
}
